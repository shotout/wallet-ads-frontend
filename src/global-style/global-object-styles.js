

const getStyle = (theme) => ({
  iconStyle: {
    height: '26px',
    width: '26px',
    color: theme.palette.primary.body
  },
  iconStyleActive: {
    color: '#fdc12c',
  }
});

export default getStyle;
